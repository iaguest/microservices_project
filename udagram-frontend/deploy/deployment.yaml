apiVersion: apps/v1
kind: Deployment
metadata:
  name: ms-frontend
  labels:
    app: ms-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ms-frontend
  template:
    metadata:
      labels:
        app: ms-frontend
    spec:
      containers:
      - name: ms-frontend
        image: iaguest/ms-frontend
        imagePullPolicy: Always  
        ports:
        - containerPort: 8080
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
          initialDelaySeconds: 3
          periodSeconds: 3
